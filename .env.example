# =============================================================================
# Gana AI Insurance - Environment Template
# =============================================================================
# Copy this file to .env and fill in your values. Do not commit .env.
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED FOR VOICE AGENT (phone calls via Twilio + OpenAI Realtime)
# -----------------------------------------------------------------------------
# OpenAI Realtime API and LLM (voice AI and claim extraction)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Twilio: get these from https://console.twilio.com (Account SID and Auth Token)
# You need a Twilio number and to set its voice webhook to: {PUBLIC_BASE_URL}/twilio/voice
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=your-twilio-auth-token

# Public URLs: Twilio’s servers must reach your app. Use your real public URL.
# Examples: ngrok (https://abc123.ngrok.io), or your server (https://voice.yourcompany.com)
# For local dev with ngrok: run `ngrok http 8000` and paste the HTTPS URL here.
PUBLIC_BASE_URL=https://your-public-host.ngrok.io
PUBLIC_WSS_BASE_URL=wss://your-public-host.ngrok.io

# -----------------------------------------------------------------------------
# OPTIONAL (have defaults; override only if needed)
# -----------------------------------------------------------------------------
# OpenAI model and voice (defaults: gpt-4o-realtime-preview, gpt-4o, shimmer)
# OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview
# OPENAI_EXTRACTION_MODEL=gpt-4o
# OPENAI_REALTIME_VOICE=shimmer

# Server bind and port (defaults: 0.0.0.0, 8000)
# HOST=0.0.0.0
# PORT=8000
# DEBUG=false

# Voice agent: silence before end-of-speech in ms (default: 600)
# SILENCE_DURATION_MS=600

# -----------------------------------------------------------------------------
# WEB CHAT / FRONTEND INTEGRATION
# -----------------------------------------------------------------------------
# CORS allowed origins for frontend connections (comma-separated)
# Default: * (allows all origins - restrict in production)
# Example: http://localhost:5173,https://your-frontend.com
# CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# -----------------------------------------------------------------------------
# NOT USED BY VOICE AGENT (for FNOL CLI / web chat only)
# -----------------------------------------------------------------------------
# ANTHROPIC_API_KEY=sk-ant-your-key-here
# LLM_PROVIDER=mock
# LLM_MODEL=
